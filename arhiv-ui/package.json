{
  "name": "@v/arhiv-ui",
  "private": true,
  "license": "MIT",
  "version": "0.0.1",
  "description": "arhiv notes",
  "main": "",
  "scripts": {
    "build": "esbuild web/index.tsx --outfile=dist/bundle.js --bundle --sourcemap --define:process.env.NODE_ENV=\\\"development\\\"",
    "build:prod": "yarn build --define:process.env.NODE_ENV=\\\"production\\\" --minify"
  },
  "dependencies": {
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  },
  "devDependencies": {
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "esbuild": "^0.8.31"
  }
}
