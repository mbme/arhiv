[package]
name = "baza"
version = "0.0.0"
edition = "2021"
authors = ["mbme"]
publish = false

[lib]
doctest = false

[lints]
workspace = true

[features]
production-mode = []

[dependencies]
anyhow.workspace = true
tracing.workspace = true

serde.workspace = true
serde_json.workspace = true

futures.workspace = true
tokio.workspace = true

aho-corasick = "1.1.0"

pulldown-cmark = { version = "0.12", default-features = false, features = ["simd", "serde"] }

tinytemplate = "1"

rusqlite = { version = "0.32", features = ["chrono", "serde_json", "functions", "collation", "bundled"] }

thiserror = "2" # macro for better errors

reqwest.workspace = true
axum.workspace = true
axum-extra.workspace = true
ordermap.workspace = true

rs-utils = { path = "../rs-utils" }

[dev-dependencies]
insta.workspace = true
