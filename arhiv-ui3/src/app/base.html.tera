<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{{ title }}</title>

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="icon" type="image/svg+xml" href="/public/favicon.svg">
    <link rel="stylesheet" href="/public/index.css">
    <script src="/public/index.js"></script>
  </head>
  <body>
    <nav id="sidebar">
      {% if show_sidebar %}
      <a href="/">index</a>

      {% for document_type in nav_document_types %}
      <a href="{{document_type.1}}">{{ document_type.0 }}s</a>
      {% endfor %}

      <a href="/new" class="flex justify-end items-center mt-8">
        <svg class="h-5 w-5 mr-1">
          <use xlink:href="#icon-document-add" />
        </svg>
        New...
      </a>

      <button
        type="button"
        class="btn btn-link"
        v-layer="/modals/scrape"
      >
        Scrape url
      </button>
      {% endif %}
    </nav>

    <main>
      {{ content | safe }}
    </main>

    <div id="modal-root"></div>

    <div id="notification-root"></div>

    {{ icons | safe }}
  </body>
</html>
