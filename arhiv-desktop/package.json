{
  "name": "arhiv-desktop-dev",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.cjs",
  "type": "module",
  "scripts": {
    "compiler-errors": "tsc --noEmit --project tsconfig.json",
    "build": "node build.ts",
    "prod:build": "NODE_ENV=production npm run build",
    "start": "electron dist/index.cjs",
    "lint": "oxlint --type-aware build.ts scripts/ src/",
    "fmt": "oxfmt build.ts scripts/ src/",
    "check-fmt": "oxfmt --check build.ts scripts/ src/",
    "test": "node --import tsx scripts/check-server-info-contract.ts"
  },
  "author": "mbme",
  "license": "ISC",
  "devDependencies": {
    "electron": "^38.3.0",
    "esbuild": "^0.27.4"
  }
}
