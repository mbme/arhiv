{
  "name": "arhiv-import",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "scrape": "node src/index.js",
    "compiler-errors": "tsc --noEmit --project tsconfig.json",
    "_build": "esbuild src/index.ts --outfile=dist/bundle.js --bundle --platform=node --define:process.env.NODE_ENV=\\\"development\\\"",
    "build": "yarn run _build --define:process.env.NODE_ENV=\\\"development\\\"",
    "prod:build": "yarn run _build --minify --define:process.env.NODE_ENV=\\\"production\\\"",
    "lint": "eslint --cache --ext .ts src"
  },
  "author": "mbme",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.2.0",
    "@typescript-eslint/parser": "^5.2.0",
    "esbuild": "^0.13.8",
    "eslint": "^8.1.0",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "puppeteer-core": "^11.0.0"
  }
}
