{
  "name": "arhiv",
  "version": "0.0.0",
  "main": "",
  "type": "module",
  "repository": "",
  "author": "mbme",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "arhiv",
    "arhiv-desktop"
  ],
  "scripts": {
    "check": "npm run compiler-errors --workspaces && npm run fmt:check:root && npm run check-fmt --workspaces && npm run lint:root && npm run lint --workspaces && npm run test --workspaces && npm run check-android-config",
    "fmt": "npm run fmt:root && npm run fmt --workspaces",
    "fmt:check": "npm run fmt:check:root && npm run check-fmt --workspaces",
    "fmt:root": "oxfmt .oxlintrc.json .oxlintrc.shared.json arhiv/.oxlintrc.json arhiv-desktop/.oxlintrc.json scripts/",
    "fmt:check:root": "oxfmt --check .oxlintrc.json .oxlintrc.shared.json arhiv/.oxlintrc.json arhiv-desktop/.oxlintrc.json scripts/",
    "lint:root": "oxlint scripts/",
    "check-android-config": "node --import tsx scripts/check-android-config.ts"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "oxfmt": "^0.43.0",
    "oxlint": "^1.58.0",
    "oxlint-tsgolint": "^0.19.0",
    "typescript": "^6.0.2",
    "tsx": "^4.20.6"
  }
}
