{
  "name": "scraper",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "compiler-errors": "tsc --noEmit --project tsconfig.json",
    "build": "tsx build.ts",
    "prod:build": "NODE_ENV=production yarn run build",
    "lint": "eslint --cache --ext .ts src/",
    "fmt": "prettier --write src/",
    "scrape": "yarn run build && node dist/node-scraper.js"
  },
  "author": "mbme",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.15.5",
    "puppeteer-core": "^15.3.2"
  },
  "dependencies": {
    "chrono-node": "^2.3.9",
    "urlpattern-polyfill": "^6.0.1"
  }
}
